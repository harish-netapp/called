name: 'Check User Membership'
on:
  workflow_call:
    inputs:
      command:
        required: true
        type: string
        description: 'Command to execute'
jobs:
  ai_code_reviewer:
    runs-on: ubuntu-latest
    steps:
      - name: test
        id: check-user
        run: |        
          echo --entrypoint "${{ inputs.command }}" -v
        shell: bash
